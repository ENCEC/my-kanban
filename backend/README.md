# Backend

## Scripts

- `npm run dev --workspace backend`
- `npm run db:init --workspace backend`
- `npm run db:seed --workspace backend`
- `npm run test --workspace backend`

## Notes

- 使用 Express + TypeScript
- SQLite 驱动为 `better-sqlite3`
- API 分为 `projects` 与 `tasks` 两组
