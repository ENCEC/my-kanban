# Frontend

## Scripts

- `npm run dev --workspace frontend`
- `npm run build --workspace frontend`
- `npm run test --workspace frontend`

## Notes

- 使用 React + Vite + TypeScript
- 看板拖拽基于 `dnd-kit`
- 默认请求后端 `http://localhost:3002`
